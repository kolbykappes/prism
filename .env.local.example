DATABASE_URL=           # Neon PostgreSQL connection string
BLOB_READ_WRITE_TOKEN=  # Vercel Blob token (auto-set when Blob store is connected)
ANTHROPIC_API_KEY=      # Claude API key for LLM summarization
INNGEST_EVENT_KEY=      # Inngest event key for background jobs
INNGEST_SIGNING_KEY=    # Inngest signing key for webhook verification
INGEST_SECRET=          # (Optional) Bearer token for external webhook callers (e.g., Zapier)
